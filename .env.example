# Used by scripts/fetch-screenshots.mjs (local screenshot refresh tool) and
# by api/screenshot.ts (Vercel Edge Function — set this in the Vercel project's
# Environment Variables dashboard for deploys, not just locally).
# Copy to .env for local use and fill in your own key — never commit the real value.
SCREENSHOT_API_KEY=your_key_here
